# Virtual Supervisor — Production Requirements (Render)
# Install with: pip install -r requirements.txt

# Flask & CORS
flask>=3.0.0
flask-cors>=4.0.0

# Firebase
firebase-admin>=6.4.0
python-dotenv>=1.0.0

# Production WSGI server
gunicorn>=21.0.0

# Detection engine (for local Phase files)
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.26.0
